"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.unregisterServiceWorker = exports.registerValidSW = exports.registerServiceWorker = void 0;
/* istanbul ignore file */
const isSafari_1 = require("./isSafari");
function registerServiceWorker(serviceWorkerPath, options = {}) {
    const { scope = '/', onServiceWorkerStateChange, onServiceWorkerRegistered } = options;
    if ((0, isSafari_1.isSafariPrivateWindow)()) {
        // Private windows in safari have a known bug with sending cookies
        // in POST requests.  This often breaks cart and checkout, so we choose
        // not to use service workers at all when in safari private windows.
        // See the bug report here: https://bugs.webkit.org/show_bug.cgi?id=186617
        console.log('skipping service worker in Safari private window');
        unregisterServiceWorker();
        return;
    }
    if ('serviceWorker' in navigator) {
        if (document.readyState === 'complete') {
            registerValidSW(serviceWorkerPath, {
                scope,
                onServiceWorkerStateChange,
                onServiceWorkerRegistered,
            });
        }
        else {
            window.addEventListener('load', () => {
                registerValidSW(serviceWorkerPath, {
                    scope,
                    onServiceWorkerStateChange,
                    onServiceWorkerRegistered,
                });
            });
        }
    }
}
exports.registerServiceWorker = registerServiceWorker;
function registerValidSW(swUrl, options = {}) {
    const { scope = '/', onServiceWorkerStateChange, onServiceWorkerRegistered } = options;
    navigator.serviceWorker
        .register(swUrl, { scope })
        .then(registration => {
        onServiceWorkerRegistered === null || onServiceWorkerRegistered === void 0 ? void 0 : onServiceWorkerRegistered(registration);
        registration.onupdatefound = () => {
            const installingWorker = registration.installing;
            if (!installingWorker)
                return;
            installingWorker.onstatechange = () => {
                console.log('[edgio service worker] state:', installingWorker.state);
                if (installingWorker.state === 'installed') {
                    if (navigator.serviceWorker.controller) {
                        // At this point, the old content will have been purged and
                        // the fresh content will have been added to the cache.
                        // It's the perfect time to display a "New content is
                        // available; please refresh." message in your web app.
                        document.dispatchEvent(new CustomEvent('sw-update-available'));
                    }
                    else {
                        // At this point, everything has been precached.
                        // It's the perfect time to display a
                        // "Content is cached for offline use." message.
                        console.log('Content is cached for offline use.');
                    }
                }
                onServiceWorkerStateChange === null || onServiceWorkerStateChange === void 0 ? void 0 : onServiceWorkerStateChange(registration);
            };
        };
    })
        .catch(error => {
        console.error('Error during service worker registration:', error);
    });
}
exports.registerValidSW = registerValidSW;
function unregisterServiceWorker() {
    if ('serviceWorker' in navigator) {
        navigator.serviceWorker.ready.then(registration => {
            registration.unregister();
        });
    }
}
exports.unregisterServiceWorker = unregisterServiceWorker;
