import Config from '../config';
import Router from '../router/Router';
import Cache from '../runtime/Cache';
export interface RuntimeObjects {
    configForEdge: Config;
    config: Config;
    router: Router;
    cache?: Cache;
}
