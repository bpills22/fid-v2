"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const http_1 = require("http");
async function prod(port) {
    const handler = (request, response) => {
        // do nothing, we just need server to listen
        // on passed port, as core connector doesn't
        // have any particular logic to run
    };
    return (0, http_1.createServer)(handler).listen(port);
}
exports.default = prod;
