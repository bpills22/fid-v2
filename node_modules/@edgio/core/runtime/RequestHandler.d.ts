import LambdaResponse from './LambdaResponse';
import LambdaRequest from './LambdaRequest';
import { RuntimeObjects } from '../lambda/RuntimeObjects';
export default class RequestHandler {
    options: RuntimeObjects;
    constructor(options: RuntimeObjects);
    handleServerless(request: LambdaRequest, response: LambdaResponse): Promise<void>;
    handleSimulator(request: LambdaRequest, response: LambdaResponse): Promise<void>;
    private handle;
    private createRequestContext;
    /**
     * Recreates the request and response objects based on redirect location value
     */
    private static recreateRequestResponse;
}
